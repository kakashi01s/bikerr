import 'package:flutter/material.dart';

class Chatroomridesscreen extends StatelessWidget {
  const Chatroomridesscreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("data"));
  }
}
